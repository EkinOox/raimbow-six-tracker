'use client';

// Page de recherche de joueurs
// Encodage: UTF-8

import PlayerSearch from '../../components/PlayerSearch/PlayerSearch';

export default function SearchPage() {
  return (
    <div className="container mx-auto py-8">
      <PlayerSearch />
    </div>
  );
}